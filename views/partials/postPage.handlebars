<nav class="navbar navbar-expand-lg navbar-light bg-light">
    <div class="container-fluid">
        <a class="navbar-brand" href="/trending">
            <img alt="FilmCult" src="../../public/assets/titleIcon.svg" class="d-inline-block align-top" id="logo">
        </a>
        <div class="collapse navbar-collapse" id="navBar Supported Content">
            <ul class="navbar-nav me-auto mb-2 mb-lg-0">
                <li class="nav-item">
                    <a class="nav-link" href="/trending">Home</a>
                </li>
                <li class="nav-item dropdown">
                    <a class="nav-link active dropdown-toggle" href="#" id="navBarDropdown" role="button"
                        data-bs-toggle="dropdown" aria-expanded="false">
                        Create
                    </a>
                    <ul class="dropdown-menu" aria-labelledby="navBarDropdown">
                        <li><a class="dropdown-item" href="/posts/createPostPage" id="postLink">Create Post</a></li>
                        <li><a class="dropdown-item" href="/movies/create" id="movieLink">Create Movie</a></li>
                    </ul>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="/search">Search</a>
                </li>
            </ul>
            <div class="d-flex" href="/user">
                <li class="nav-item dropdown">
                    <a class="nav-link dropdown-toggle" href="#" id="navBarUserDropdown" role="button"
                        data-bs-toggle="dropdown" aria-expanded="false">
                        <img alt="User Profile" src="../../public/assets/userIcon.svg">
                    </a>
                    <ul class="dropdown-menu dropdown-menu-end" aria-labelledby="navBarUserDropdown">
                        <li><a class="dropdown-item" href="/users" id="userLink">User Profile</a></li>
                        <li><a class="dropdown-item" href="/logout" id="logoutLink">Log Out</a></li>
                    </ul>
                </li>
            </div>
        </div>
    </div>

</nav>

<div class="container">
    <div class="row">
        <div class="col-md-2">
            <form action="/edits/{{post._id}}" method="GET">
                <button class="btn btn-secondary">Edit Post</button>
            </form>

            <form action="/posts/delete/{{post._id}}" method="GET">
                <button class="btn btn-secondary" type="submit">Delete Post</button>
            </form>
        </div>
        <div class="col-md-6">
            <div class="postCard">
                <div class="card">
                    <h4>{{post.postTitle}}</h4>
                    <h6>By <a href="/users">user</a> on movie <a
                            href="/movies/detail/{{post.postMovieId}}">{{movie.title}}</a>
                        <div class="postImg"><img alt={{post.postTitle}} src={{post.postImage}}></div>
                        <hr>
                    </h6>
                    <h5></h5>
                    <p class="postBody">{{post.postBody}}</p>
                    <p class="likes">{{post.postLikes}} {{post.postDislikes}}</p>


                    <span class="likebtn-wrapper" data-theme="likebtn" data-identifier="item_1"></span>
                    <script>(function (d, e, s) {
                            if (d.getElementById("likebtn_wjs")) return;
                            a = d.createElement(e);
                            m = d.getElementsByTagName(e)[0];
                            a.async = 1; a.id = "likebtn_wjs"; a.src = s; m.parentNode.insertBefore(a, m)
                        })(document, "script", "//w.likebtn.com/js/w/widget.js");</script>
                </div>
            </div>

            <div>
                <div>
                    {{#each comments}}
                    <div class="container">
                        <div class="col-sm">
                            <div class="commentCard">
                                <div class="card">
                                    <p class="commentName">{{this.userName}}</p>
                                    <p>{{this.body}}</p>
                                </div>
                            </div>
                        </div>
                    </div>
                    {{/each}}
                </div>

                <form action="/comments" method="POST" class="createComment">
                    <label id="titleLabel">
                        <input type="text" name="commentBody" title="body">
                    </label>
                    <input type="hidden" name="postID" value={{post._id}} />
                    <button class="btn btn-primary" type="submit">Add Comment</button>
                </form>
            </div>
        </div>
        <div class="col-md-4">
            <div class="card">
                <div class="movieCard">
                    <h5><a href="/movies/detail/{{post.postMovieId}}">{{movie.title}}</a></h5>
                    <div><img src={{movie.image}} alt={{movie.title}}></div>
                    <div>
                        <p>{{movie.description}}</p>
                        <a href="/movies/detail/{{post.postMovieId}}">See all Posts about {{movie.title}}
                            here</a>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>

{{!-- <div class="container">
    <div class="row">
        <div class="col-md-3"></div>
        <div class="col-md-6">
            <div class="card">
                <li>
                    <a href="/posts/{{this._id}}">{{this.postTitle}}</a>
                    <ul>
                        <li>
                            {{this.postBody}}
                        </li>
                    </ul>
                </li>
            </div>
        </div>
        <div class="col-md-3"></div>
    </div>
</div> --}}